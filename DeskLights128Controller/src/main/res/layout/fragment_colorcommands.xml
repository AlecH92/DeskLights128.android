<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ColorCommandsFragment" >

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Rainbow"
        android:id="@+id/rainbowButton"
        android:layout_marginTop="30dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_marginRight="30dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Random"
        android:id="@+id/randomButton"
        android:layout_below="@+id/rainbowButton"
        android:layout_alignRight="@+id/rainbowButton"
        android:layout_alignEnd="@+id/rainbowButton"
        android:layout_marginTop="25dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="K.I.T.T."
        android:id="@+id/kittButton"
        android:layout_below="@+id/randomButton"
        android:layout_alignRight="@+id/randomButton"
        android:layout_alignEnd="@+id/randomButton"
        android:layout_marginTop="25dp" />
</RelativeLayout>